{
  "name": "maison-velours",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently --names \"API,WEB\" --prefix-colors \"blue.bold,green.bold\" \"npm run start:api\" \"npm run start:web\"",
    "start:api": "run-backend.bat",
    "start:web": "npm --prefix frontend start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
